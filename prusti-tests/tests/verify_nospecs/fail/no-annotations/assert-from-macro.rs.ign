macro_rules! my_assert {
    ( $( $args:expr ),* ) => {
        /*assert*/drop( $( $args ),* )  // ERRXR: the asserted expression might not hold
    };
}

fn foo(x: bool) {
    my_/*assert*/drop(x);
}

fn main() {}
